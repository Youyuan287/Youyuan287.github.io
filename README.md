# Youyuan Tang — Academic Homepage

This site uses the Jekyll academic homepage template structure.

The homepage content is in `_pages/youyuan.md`. Personal information is configured in `_config.yml`, and the navigation is in `_data/navigation.yml`.

The original static page is preserved as `index-legacy.html`.
